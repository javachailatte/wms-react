const initialState = [
    {
        isLoggedIn: false
    }
];


export default function stuff(state = initialState, action) {
    switch(action.type) {
        default:
            return state;
    }
}